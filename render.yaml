services:
  - type: web
    name: prestes-simulador-api
    runtime: python
    buildCommand: pip install -r leads_model/requirements.txt
    startCommand: cd leads_model && uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
